import type { AudioPlayerProps } from '../types'

// Web stub implementation - audio playback not supported in web environment
export function AudioPlayerWeb(_props: AudioPlayerProps) {
  // Audio playback not supported in web environment
  // Return null to render nothing
  return null
}
